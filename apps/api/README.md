# @cricapp/api

NestJS HTTP API. Independently deployable from `services/ingestion`. Scaffold Nest here when you start Sprint work on read APIs; import `@cricapp/shared-types` for `Match` / `Innings`.
