/** @see schema.js — keep values in sync with ODDS_* there. */
export const ODDS_LICENSE_STATUS = Object.freeze({
  LICENSED: 'licensed',
  PENDING: 'pending',
  DISABLED: 'disabled',
});

export const ODDS_MARKET_TYPE = Object.freeze({
  MATCH_WINNER: 'match_winner',
});

export const ODDS_SELECTION = Object.freeze({
  HOME: 'home',
  AWAY: 'away',
  DRAW: 'draw',
});
