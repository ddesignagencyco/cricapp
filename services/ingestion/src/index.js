import { PROVIDERS } from "./schemas.js";

export { computeRunRate, normalizeMatch } from "./normalize.js";
export { diffMatch } from "./diff.js";

console.log(`cricapp ingestion — provider=${PROVIDERS.SPORTRADAR}; poll loop not wired yet`);
